# tag_cleanup
This script fetches remote tags and deletes them.

It is composed so it first removes tags you want to KEEP and then REMOVES all of the others. 

USE WITH CAUTION!


Example use:

```
./delete_tags.sh v0.1.0
```
